import React, { Component } from 'react';
import PropTypes from 'prop-types';
import './style.css';

const Tree = ({ files }) => {
	return (
		<div className="tree">

		</div>
	);
};

export default Tree;
